name: Change shell
on: push
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - shell: bash     # シェルにBashを設定
        run: echo "${SHELL}"
      - shell: python   # シェルにPythonを設定
        run: |
          import os
          print(os.environ['PATH'])